Native Launcher Placeholder
===========================

This directory is reserved for a future packaged `entroly-rs` binary.

The public `entroly` command currently defaults to the Python CLI. Set
`ENTROLY_USE_RUST_LAUNCHER=1` to opt into a real native binary when one is
installed here or pointed to by `ENTROLY_RUST_BIN`.
